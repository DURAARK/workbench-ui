import { getBasename } from '../../../helpers/get-basename';
import { module, test } from 'qunit';

module('Unit | Helper | get basename');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getBasename(42);
  assert.ok(result);
});
