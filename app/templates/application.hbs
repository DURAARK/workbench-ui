{{#if isLoading}}
	{{#if loadingMessage}}
		<!-- <div class="loading-message"> -->
			<!-- <h3>{{loadingMessage}}</h3> -->
			{{loading-spinner-fullscreen}}
		<!-- </div> -->
	{{/if}}
{{/if}}

<div class="l-canvas">
	{{outlet}}
</div>
