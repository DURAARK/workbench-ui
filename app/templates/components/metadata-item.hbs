{{#if isBuildm}}
	{{#if isLoading}}
		<li class="da-file-entry"><div class="da-valid btn btn-blue" title="Schema">{{loading-spinner}}</div>Descriptive metadata</li>
	{{else}}
		<li class="da-file-entry"><div class="da-valid btn btn-blue" title="Schema">{{item.schema}}</div>Descriptive metadata</li>
	{{/if}}
{{/if}}

{{#if isIfcm}}	
	{{#if isLoading}}
		<li class="da-file-entry"><div class="da-valid btn btn-alt" title="Schema">{{loading-spinner}}</div>Technical metadata</li>
	{{else}}
		<li class="da-file-entry"><div class="da-valid btn btn-alt" title="Schema">{{item.schema}}</div>Technical metadata</li>
	{{/if}}
{{/if}}

{{#if isE57m}}
	{{#if isLoading}}
		<li class="da-file-entry"><div class="da-valid btn btn-green" title="Schema">{{loading-spinner}}</div>Technical metadata</li>
	{{else}}
		<li class="da-file-entry"><div class="da-valid btn btn-green" title="Schema">{{item.schema}}</div>Technical metadata</li>
	{{/if}}
{{/if}}