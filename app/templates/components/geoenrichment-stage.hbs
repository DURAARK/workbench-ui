<br>
<center><div class="da-valid btn btn-blue" {{action "editStage" stage}}>Edit</div></center>

{{!-- <section class="post-controls">
    <a id="ember891" class="ember-view btn btn-default post-edit" href="/ghost/editor/1/"> Edit</a>
</section> --}}

{{!-- <br><br>

{{!-- {{#if stage.isLoading}}
	{{loading-spinner-center}}
{{/if}}

{{#if stage.metadata.length}}

<ul>
	{{#each item in stage.metadata}}
		{{#if item}}
			{{geoenrichment-item item=item isLoading=stage.isLoading}}
		{{/if}}
	{{/each}}
</ul>

{{else}}

<div>Requesting metadata ...</div>

{{/if}} --}}