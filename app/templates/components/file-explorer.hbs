<ol class="fe-list">
	{{#if this.showOnlySelected}}

		{{#each selectedItems}}
			{{selectable-item item=this action="handleItemSelected"}}
		{{/each}}

	{{else}}

		{{#each deselectedItems}}
			{{selectable-item item=this action="handleItemSelected"}}
		{{/each}}

	{{/if}}
</ol>