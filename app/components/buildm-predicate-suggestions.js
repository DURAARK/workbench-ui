import Ember from 'ember';

export default Ember.Component.extend({
  // predicate: null,
  // suggestions: [],
  //
  // onPredicateChanged: function() {
  //   let predicate = this.get('predicate');
  //
  //   this.duraark.getItemsForPredicate(predicate).then(items => {
  //     this.set('suggestions', items);
  //   });
  // }.observes('predicate').on('init'),
  //
  // actions: {
  //   selectItem(item) {
  //     console.log('selected: ' + item);
  //   }
  // }
});
